# Яндекс Диск - Просмотр файлов и директорий

Скрипт для просмотра директорий и файлов на Яндекс Диске через API.

## Установка

1. Создайте виртуальное окружение (если еще не создано):
```bash
python3 -m venv .venv
```

2. Активируйте виртуальное окружение:
```bash
# На macOS/Linux:
source .venv/bin/activate

# На Windows:
.venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Убедитесь, что файл `.env` содержит необходимые данные:
   - `YANDEX_DISK_CLIENT_ID` - Client ID приложения (уже настроен)
   - `YANDEX_DISK_CLIENT_SECRET` - Client Secret (уже настроен)
   - `YANDEX_DISK_OAUTH_TOKEN` - OAuth токен для доступа к API

## Получение OAuth токена

Если у вас нет OAuth токена или он истек, выполните следующие шаги:

### Способ 1: Автоматический (рекомендуется)

Запустите скрипт для получения токена:
```bash
python get_oauth_token.py
```

Скрипт откроет браузер с формой авторизации. После авторизации:
1. Скопируйте токен из URL (часть после `access_token=` и до `&`)
2. Обновите файл `.env` с новым токеном

### Способ 2: Ручной

1. Откройте в браузере следующую ссылку:
   ```
   https://oauth.yandex.ru/authorize?response_type=token&client_id=047b883acf9042e2a85f901255b81520
   ```

2. Войдите в свой аккаунт Яндекс (если не авторизованы)

3. Нажмите "Разрешить" для предоставления доступа приложению

4. После авторизации вы будете перенаправлены на страницу с токеном в URL:
   ```
   https://oauth.yandex.ru/verification_code#access_token=ВАШ_ТОКЕН&token_type=bearer
   ```

5. Скопируйте токен (часть после `access_token=` и до `&`)

6. Обновите файл `.env`:
   ```
   YANDEX_DISK_OAUTH_TOKEN=ваш_новый_токен
   ```

**Важно:**
- Токен действителен 1 год
- Не делитесь токеном с другими
- Храните `.env` файл в безопасности (не коммитьте в git!)

## Использование

### Веб-интерфейс (GUI) - Рекомендуется 🌐

Запустите веб-интерфейс для удобного просмотра файлов в браузере:

```bash
# С активированным виртуальным окружением:
python yandex_disk_gui.py

# Или напрямую через .venv:
.venv/bin/python yandex_disk_gui.py
```

После запуска откройте браузер и перейдите по адресу: **http://127.0.0.1:5000**

**Возможности веб-интерфейса:**
- 📁 Просмотр директорий и файлов
- 🗂️ Навигация по папкам (клик по папке)
- 📊 Статистика (количество папок и файлов)
- 🍞 Хлебные крошки (breadcrumb) для навигации
- 📱 Адаптивный дизайн

### Консольный интерфейс

Запустите скрипт для просмотра в терминале:
```bash
# С активированным виртуальным окружением:
python list_yandex_disk.py

# Или напрямую через .venv:
.venv/bin/python list_yandex_disk.py
```

Скрипт покажет все директории и файлы в корне вашего Яндекс Диска.

## Структура проекта

- `.env` - файл с конфиденциальными данными (не коммитьте в git!)
- `yandex_disk_gui.py` - **веб-интерфейс (GUI) для просмотра файлов** 🌐
- `list_yandex_disk.py` - консольный скрипт для работы с API
- `get_oauth_token.py` - скрипт для получения OAuth токена
- `requirements.txt` - зависимости Python

## Устранение проблем

### Ошибка "Токен недействителен или истек"
1. Получите новый токен (см. раздел "Получение OAuth токена")
2. Обновите `YANDEX_DISK_OAUTH_TOKEN` в файле `.env`

### Ошибка "Тип аккаунта не поддерживается"
- Убедитесь, что ваш аккаунт Яндекс имеет доступ к Яндекс Диску
- Проверьте, что вы используете правильный Client ID
- Попробуйте получить новый токен

